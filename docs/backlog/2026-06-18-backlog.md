# Backlog FoodFlow — itens adiados (18/06/2026)

Registro do que foi **deixado para depois** ao fechar o **Site V1**. Próximo foco
combinado: **AURA / sistema de atendimento** (Agente de Vendas com IA), com
**coerência/dogfooding** — a própria FoodFlow usar a AURA pra atender os leads
do site.

---

## 1. Site `foodflow.ia.br` — V1 concluído; adiados (polish, não bloqueiam)

- **Aviso de novo lead + acompanhamento (status Novo/Em contato/Convertido/Descartado, botão "Falar no WhatsApp")** → **NÃO** fazer como CRM separado; **migrar para a AURA** (coerência: a IA que vendemos atende nossos próprios leads).
- **SEO/compartilhamento:** imagem de preview (og-image), favicon próprio, `robots.txt`, `sitemap.xml`, canonical.
- **Analytics de conversão:** escolher ferramenta (Plausible / GA) e medir visita × lead. *(decisão do dono: qual ferramenta)*
- **Depoimento do caso Rancho (Hero 4):** frase do dono + print real de uma conversa da AURA (alta credibilidade). Hoje só os números (13/18/36%).
- **Teste end-to-end do formulário:** enviar lead real → ver na aba Leads.
- **Mobile:** drag-and-drop dos planos funciona só no desktop (HTML5 drag); se precisar ordenar no celular, add setas ↑/↓.

### Dados a preencher no painel (não-código, dono)
- Marcar **"Mais popular"** no plano certo; definir **ordem** (arrastar); preencher **benefícios** do Premium; vincular **módulos do Básico** (hoje sai "Acesso à plataforma"); revisar **preço/ciclo**.

---

## 2. Cobrança / Mercado Pago (control plane)

- **Teste ponta-a-ponta no sandbox** (gerar link → autorizar cartão de teste → webhook → conta ATIVA). Manual, com o dono.
- **Trial automático na conta:** ao atribuir plano com `diasTeste`, a assinatura entra em **TESTE** com `trialAte = hoje + dias`; **rebaixe automático** pro grátis por inadimplência (cron).
- **Validar webhook do MP por secret** (hoje a segurança vem de reconsultar o MP).
- **Trocar token de TESTE pelo de PRODUÇÃO** (`FOODFLOW_MP_ACCESS_TOKEN`) quando for cobrar de verdade.
- **Marketplace:** módulos avulsos (`AssinaturaModulo`) — comprar módulo solto fora do plano.

---

## 3. Plataforma / multi-tenant

- **De-privilegiar o Rancho** (domínio próprio + resolução estrita de tenant + tenant em webhooks/jobs) + **RLS** — fazer **antes do 2º cliente real** com dados.

---

## 4. AURA / Atendimento — PRÓXIMA FASE (foco)

Sistema de atendimento "Agente de Vendas com IA" **coerente com o que vendemos**.
- **Base existente a reaproveitar:** backend `apps/backend/src/agentes/` (classificador, guardrails, sentimento, sanitizer) + `conversacao.service` (WhatsApp/Evolution) + **mineração/prospecção** (`mineracao.service`). Repo externo **`quadradois/genesis`** (assistente "Nox" desktop) a adaptar.
- **Planejar AS-IS → TO-BE** (Arquitetura de Intenção) antes de codar.
- **Dogfooding:** a FoodFlow usa a AURA pra atender/qualificar os leads do `foodflow.ia.br`.

---

## 5. Pendências antigas (Rancho)

- **Bug bairro Aparecida:** parser da importação de imóveis não mapeia `nome_bairro`.
- **Cutover WhatsApp** (Evolution Go): conectar instância (QR/telefone).

---

## 6. Infra / CI (técnico)

- **Flake do CI:** `pnpm install` às vezes trava no runner do GitHub → `gh run cancel` + `gh run rerun`. Avaliar **endurecer o passo** (retry / cache `pnpm fetch`).
- Actions em Node 20 (deprecando) — atualizar quando der.
